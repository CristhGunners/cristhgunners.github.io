export type PostItems = {
  [key: string]: string;
};

export type TagItem = {
  tag: string;
  count: number;
};

export type ProjectItem = {
  title: string;
  description: string;
  url: string;
  logo: string;
};
